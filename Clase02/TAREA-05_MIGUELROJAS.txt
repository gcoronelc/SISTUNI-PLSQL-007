CREATE OR REPLACE FUNCTION CALCULA_SUMA(P1 NUMBER, P2 NUMBER)
RETURN NUMBER
AS
    V_TOTAL NUMBER(10,2) := 0;
BEGIN
    FOR N IN P1 .. P2 LOOP
        V_TOTAL := V_TOTAL + N;
        
    END LOOP;
    DBMS_OUTPUT.PUT_LINE(V_TOTAL);
    
    RETURN V_TOTAL;
END;